import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smarthome.control',
  appName: 'Smart Home Control',
  webDir: 'dist'
};

export default config;
